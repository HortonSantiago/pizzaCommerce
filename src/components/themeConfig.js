import { createTheme } from "@mui/material";

export const CustomTheme = createTheme({
  palette: {
    primary: {
      main: "#d87d4a",
    },
    secondary: {
      main: "#d87d4a",
    },
  },
});
